[phases.setup]
nixPkgs = ["python313", "freetype", "libpng", "libjpeg"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"
